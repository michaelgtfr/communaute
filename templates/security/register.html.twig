{% extends 'base.html.twig' %}

{% block body %}
    <section class="form">
        {{ form_start(form) }}

        <div class="text-center">
            <button type="submit" class="btn btn-primary">Valider</button>
        </div>

        {{ form_end(form) }}
    </section>
{% endblock %}